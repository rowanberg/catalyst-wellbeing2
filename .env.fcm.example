# Firebase Cloud Messaging (FCM) Environment Variables
# Add these to your .env.local file

# Get these values from Firebase Console:
# 1. Go to https://console.firebase.google.com/
# 2. Select your project (or create a new one)
# 3. Go to Project Settings (gear icon) → General
# 4. Scroll down to "Your apps" section
# 5. If you don't have a web app, click "Add app" → Web (</>)
# 6. Copy the firebaseConfig values

NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSy...
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789:web:abcdef

# VAPID Key for Web Push (Required for FCM web notifications)
# 1. In Firebase Console, go to Project Settings → Cloud Messaging
# 2. Scroll to "Web Push certificates"
# 3. Click "Generate key pair" if you don't have one
# 4. Copy the key pair value

NEXT_PUBLIC_FIREBASE_VAPID_KEY=BNq...

# ============================================
# SETUP INSTRUCTIONS
# ============================================

# Step 1: Create Firebase Project
# - Go to https://console.firebase.google.com/
# - Click "Add project"
# - Follow the setup wizard

# Step 2: Enable Cloud Messaging
# - In Firebase Console, go to Build → Cloud Messaging
# - Enable the Cloud Messaging API if prompted

# Step 3: Get Web App Config
# - Project Settings → General → Your apps
# - Add a web app or use existing
# - Copy config values above

# Step 4: Generate VAPID Key
# - Project Settings → Cloud Messaging → Web Push certificates
# - Generate key pair
# - Copy the key

# Step 5: Update Service Worker
# - Edit public/firebase-messaging-sw.js
# - Replace placeholder values with your actual Firebase config

# Step 6: Run Supabase Migration
# - Run the SQL migration: supabase\migrations\create_fcm_tokens_table.sql
# - This creates the fcm_tokens table in your database

# Step 7: Install Dependencies (if not already installed)
# npm install firebase

# Step 8: Test Notifications
# - Go to /student dashboard
# - Click "Enable Notifications" button
# - Accept browser permission prompt
# - Token should be saved to fcm_tokens table

# ============================================
# TESTING
# ============================================

# Test sending a notification from Firebase Console:
# 1. Go to Engage → Messaging
# 2. Create campaign → Send test message
# 3. Add FCM registration token from fcm_tokens table
# 4. Send test notification
