# ============================================
# Capacitor APK - Environment Variables
# ============================================
# Copy this to .env.production for Capacitor builds
# ============================================

# Backend API URL (where your API routes are deployed)
# This is where SendGrid, Gemini, and server-side logic runs
NEXT_PUBLIC_API_URL=https://catalyst-backend.netlify.app

# Supabase (works directly in APK)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# App Configuration
NEXT_PUBLIC_APP_ENV=production
NEXT_PUBLIC_SITE_URL=https://catalyst-backend.netlify.app

# ============================================
# How It Works:
# ============================================
# 1. Frontend (APK) - Static HTML/CSS/JS with Supabase client
#    - Deployed via Capacitor to Android/iOS
#    - Uses NEXT_PUBLIC_API_URL to call backend
#
# 2. Backend (Netlify/Vercel) - API routes only
#    - Handles SendGrid emails
#    - Handles Gemini AI
#    - Handles admin operations
#    - Uses service role key (server-side)
# ============================================
